{
  "name": "fba-tracker",
  "private": true,
  "scripts": {
    "build": "esbuild *.jsx --outdir=compiled --minify",
    "watch": "esbuild *.jsx --outdir=compiled --watch",
    "prestart": "esbuild *.jsx --outdir=compiled --minify",
    "start": "python3 server.py"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
