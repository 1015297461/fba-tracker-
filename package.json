{
  "name": "fba-tracker",
  "private": true,
  "scripts": {
    "build": "babel *.jsx --out-dir compiled",
    "watch": "babel *.jsx --out-dir compiled --watch",
    "prestart": "babel *.jsx --out-dir compiled",
    "start": "python3 server.py"
  },
  "devDependencies": {
    "@babel/cli": "^7.26.4",
    "@babel/core": "^7.26.0",
    "@babel/preset-react": "^7.25.9"
  }
}
